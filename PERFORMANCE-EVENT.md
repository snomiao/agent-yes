# PERFORMANCE-EVENT 2026-08-13

システム負荷調査で発見された agent-yes 関連プロセスのリソースリーク (2 件)。

## 1. 旧バージョンの agent-yes バイナリが多数残存

エージェントの CLI バイナリがバージョンアップ毎に `~/.cache/agent-yes/bin/<ver>/` に積み重なり、
古いバージョンの常駐プロセスが終了エージェントと共に残っている。

現在稼働中のバージョン分布 (プロセス数 / バージョン):

| バージョン                  | プロセス数 |
| --------------------------- | ---------- |
| 1.272.0 (現行)              | 28         |
| 1.256.1                     | 7          |
| 1.254.0                     | 7          |
| 1.269.1                     | 4          |
| 1.270.1                     | 3          |
| 1.267.2                     | 3          |
| 1.267.0                     | 2          |
| 1.265.4                     | 2          |
| 1.265.0                     | 2          |
| 1.271.0 / 1.262.0 / 1.254.1 | 各 1       |

旧版 (1.272.0 以外) 合計: **34 プロセス / 約 222 Mi RSS**。
全 agent-yes 関連 (cargo 版 `~/.cargo/bin/agent-yes` 含む): **86 プロセス / 約 1.06 Gi RSS**。

## 2. `ayrs` daemon が子プロセスを reap していない (zombie)

`ayrs serve` (PID 21140, PPID=1) が 7 個の zombie 子プロセスを放置している。

| zombie PID                                      | parent               |
| ----------------------------------------------- | -------------------- |
| 25285, 25671, 26960, 68107, 72873, 91768, 93883 | 21140 (`ayrs serve`) |

zombie 自体は RSS を消費しないが、プロセステーブルを汚染し続ける。
`ayrs` が子エージェント終了時に `waitpid` / 子プロセスの reap を正しく行っていない。

## 調査依頼

1. 旧版バイナリ常駐の抑制: バージョンアップ時に旧版プロセスが残る設計か。終了エージェントの旧版プロセスを回収する仕組みはあるか。キャッシュのバージョン掃除 (GC) を入れるべきか。
2. `ayrs serve` の zombie 発生経路: 子プロセス (agent) 終了時の reap が漏れている箇所。SIGCHLD ハンドリング / `waitpid(-1, ...)` の有無。

## 3. 負荷の真因は旧版調査のデバッグスクリプト残骸 (外部要因・2026-08-13 追加)

負荷の最大要因は repo のコードではなく、過去の agent-yes hang 調査が残したデバッグ
スクリプトの孤児プロセスだった。

- プロセス: `bun /var/folders/…/T/sv-hang-XXXX/hang-ay.ts ls --local --json`
  (2 プロセス、8/12 11:42 起動、約 1 日 6 時間稼働、各 1 CPU コアを専有、PPID=1)
- `hang-ay.ts` の内容は `await new Promise(() => {})` — 永久に resolve しないスタブ。
- `/var/folders/…/T/` 下に `sv-hang-*` ディレクトリが 53 個残存 (8/12 11:39〜12:33 生成)。
- 影響: load を 40 → 43 に押し上げていた。両プロセス kill 後 load は 29 に低下。
- 対応: プロセス kill 済み。残骸ディレクトリも削除。

補足: `ts/cli.ts` の `runSubcommand` にグローバルな watchdog / command timeout は
存在しない。`ay ls` 自体はローカルファイル参照で即時復帰するが、`--local` を
付けない場合のリモート fetch (`runRemoteLs`) にはタイムアウトがなく、応答しない
remote 相手に引っかかると長時間ブロックし得る。上記スタブはこの調査の spawn 側が
timeout / reap を持たずに放置した残骸であり、agent-yes 本体のリークではない。

（このファイルは 2026-08-13 のシステム負荷調査で生成された）
